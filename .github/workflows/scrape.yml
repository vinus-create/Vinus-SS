name: ShopeeScope Daily Scrape

on:
  schedule:
    - cron: '0 0 * * *'   # 每天 8:00 AM MYT (UTC+8 → 0:00 UTC)
  workflow_dispatch:        # 允许手动触发

jobs:
  scrape-batch-0:
    name: "Batch 0 (shops 1-7)"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: scraper/package.json

      - name: Install dependencies
        working-directory: scraper
        run: npm install

      - name: Install Playwright Chromium
        working-directory: scraper
        run: npx playwright install chromium --with-deps

      - name: Run scraper (batch 0)
        working-directory: scraper
        env:
          SHOP_BATCH: '0'
          SUPABASE_URL:        ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          VERCEL_URL:          ${{ secrets.VERCEL_URL }}
        run: node scrape.js

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scrape-failure-logs-batch0
          path: scraper/*.log
          if-no-files-found: ignore

  scrape-batch-1:
    name: "Batch 1 (shops 8-13)"
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: scraper/package.json

      - name: Install dependencies
        working-directory: scraper
        run: npm install

      - name: Install Playwright Chromium
        working-directory: scraper
        run: npx playwright install chromium --with-deps

      - name: Run scraper (batch 1)
        working-directory: scraper
        env:
          SHOP_BATCH: '1'
          SUPABASE_URL:        ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          VERCEL_URL:          ${{ secrets.VERCEL_URL }}
        run: node scrape.js

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scrape-failure-logs-batch1
          path: scraper/*.log
          if-no-files-found: ignore
